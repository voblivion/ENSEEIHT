function s = pasDeCauchy(f, g, h, delta)
    a = 1/2 * g' * h * g;
    ng = norm(g);
    b = - ng^2;
    c = f;
    
    % Calcul de s
    if a > 0
        t = - b / (2 * a);
        
    else
        if b < 0
     end
    
    s = - t * g;
end